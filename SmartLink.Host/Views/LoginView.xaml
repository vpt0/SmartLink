<Window x:Class="SmartLink.Host.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="350" Width="320" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Login" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20" Grid.Row="0"/>
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Email"/>
            <TextBox x:Name="txtEmail" Height="28"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Password"/>
            <PasswordBox x:Name="txtPassword" Height="28"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,10">
            <CheckBox x:Name="chkRememberMe" Content="Remember Me"/>
            <Button Content="Forgot Password?" Margin="10,0,0,0" Click="ForgotPassword_Click"/>
        </StackPanel>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Login" Width="100" Margin="0,0,10,0" Click="Login_Click"/>
            <Button Content="Create Account" Width="120" Click="CreateAccount_Click"/>
        </StackPanel>
    </Grid>
</Window>